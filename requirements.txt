aiohttp>=3.9.0
yarl>=1.9.0
maxapi-python>=2.4.1
python-telegram-bot>=21.0
python-dotenv>=1.0.0
httpx[socks]>=0.24.0
Pillow>=10.0.0
aiosqlite>=0.19.0
# Ships an ffmpeg binary, used to repackage Telegram voice notes into the
# container MAX accepts (see app/pymax_client.py). A system ffmpeg is
# preferred when present — the wheel here is glibc-linked, so on the
# musl-based Docker image the one from apk is the one that runs.
imageio-ffmpeg>=0.4.9
